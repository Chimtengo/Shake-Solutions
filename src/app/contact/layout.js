import { pageMetadata } from '@/lib/seo'

export const metadata = pageMetadata({
  title: 'Contact Shake Solutions',
  description:
    'Contact Shake Solutions in Lilongwe, Malawi for web development, hosting, domain registration, networking, digital marketing, and ICT support.',
  path: '/contact'
})

export default function ContactLayout({ children }) {
  return children
}

